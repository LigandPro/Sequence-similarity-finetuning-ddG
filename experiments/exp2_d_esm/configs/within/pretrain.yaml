MODEL:
  DECODER_LAYERS: 2
  ENCODER_LAYERS: 1
  ESM_CHECKPOINT: facebook/esm2_t6_8M_UR50D
  MLP_LAYERS: 3
  PRECODER_LAYERS: 1
  SELFATT_DIM_EXPANSION: 2

TRAIN:
  DATASET_PATH: "data/splits/exp2_d_esm/within/pretrain.tsv"
  MAX_STEPS: 8500
  EVAL_STEPS: 100
  EARLY_STOPPING_PATIENCE_STEPS: 
  BATCH_SIZE: 400

  LR: 3e-4
  DROPOUT: 0.0

TEST:
  BATCH_SIZE: 400
  DATASETS:
    - ["heldout_val", "data/splits/exp2_d_esm/within/heldout_val.tsv", false]
  TARGET_DATASET: "heldout_val"
